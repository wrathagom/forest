# Claude jsonl fixtures

One representative line per file. Synthetic — constructed to match the
documented Claude jsonl shape, not captured from real history. Used by
`parser.test.ts`. Refresh from real `~/.claude/projects/` (with careful
sanitization) when claude's format drifts; keep filenames stable so tests
don't churn.
